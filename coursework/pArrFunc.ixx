export module pArrFunc;
import <iostream>;
import <string>;
import <fstream>;
import <conio.h>;
import list;
export namespace pArrFunc {
    //using namespace List;
    unsigned int arrSize = List::COUNT_NODE;
    /*void Create(Node<string>* node) {
        while (node != nullptr) {
            cout << node->getStr() << endl;
            node = node->getNext();
        }
    }*/
}